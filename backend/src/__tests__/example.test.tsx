import { test, expect, describe } from 'vitest';

describe('example', () => {
  test('example test 1', () => {
    expect(true).toBeTruthy();
  });

  test('example test 2', () => {
    expect(false).toBeFalsy();
  });
});
