import { test, expect, describe } from 'vitest';

describe('<App />', () => {
  test('example test 1', () => {
    expect(true).toBeTruthy();
  });

  test('example test 2', () => {
    expect(false).toBeFalsy();
  });
});
